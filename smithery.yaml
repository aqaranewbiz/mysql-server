version: '1.0'
build:
  dockerfile: Dockerfile
configSchema:
  type: "object"
  properties: {}
  required: []
startCommand:
  type: "stdio"
  commandFunction: |
    function getCommand(config) {
      return {
        command: "python",
        args: ["mcp_server.py"]
      };
    } 